ipso.path=${ipso.path}
solver.path=${solver.path}
ampl.path=${ampl.path}
solverResources.path=${solverResources.path}
contingencies.path=${contingencies.path}
network.folder=${network.folder}
network.prefix=${network.prefix}
network.path=${network.path}
network.config=${network.config}

