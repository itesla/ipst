The function Module00(path,input) is the main

The function AllSeparation(caso,newsum,fig,path) runs alone, it divides the sum between the 2 or 3 clustered variables

The function Disegna(array,path,allplot) runs alone, it plots some graphs of the variable that you want.
